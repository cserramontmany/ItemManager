// if more string fields are added add them on AppConst.orderFields
export class Item{
        title: string;
        description: string;
        price: string;
        email: string;
        image: string;
}